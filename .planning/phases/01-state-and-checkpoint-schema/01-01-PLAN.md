---
phase: 01-state-and-checkpoint-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roboclaws/agents/task_state.py
  - tests/unit/agents/test_task_state.py
autonomous: true
requirements:
  - REQ-state-and-checkpoint-schema
must_haves:
  truths:
    - "A task snapshot round-trips through JSON without losing task/intent, pose/waypoint, object freshness/provenance, action outcomes, safety, completion, evidence digests, or revision."
    - "Snapshot serialization excludes raw prompts, credentials, private scoring truth, and full tool payloads."
    - "The schema module provides atomic checkpoint persistence before any runtime hook consumes it."
  artifacts:
    - path: roboclaws/agents/task_state.py
      provides: "Dependency-light typed snapshot/checkpoint models, privacy-bounded serialization, and atomic persistence"
    - path: tests/unit/agents/test_task_state.py
      provides: "Round-trip, revision, stale-observation, and privacy contract tests"
  key_links:
    - from: roboclaws/agents/task_state.py
      to: tests/unit/agents/test_task_state.py
      via: "public constructors and from/to JSON APIs"
      pattern: "TaskSnapshot|Checkpoint"
---

<objective>
Define the canonical privacy-bounded task snapshot and checkpoint schema that later context assembly and continuation work will consume.

Purpose: make action-critical state durable and typed while preserving existing artifact and MCP ownership.
Output: `task_state.py` plus focused schema tests.
</objective>

<context>
@.planning/phases/01-state-and-checkpoint-schema/01-CONTEXT.md
@docs/plans/2026-09-01-state-first-context-manager.md
@.planning/research/context-management/report.md
@roboclaws/agents/drivers/openai_agents_event_projection.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define typed snapshot and checkpoint contracts</name>
  <files>roboclaws/agents/task_state.py, tests/unit/agents/test_task_state.py</files>
  <behavior>
    - Snapshot round-trip preserves task/intent, robot pose and waypoint, object handles with freshness/provenance, action outcomes, safety gates, completion, evidence references/digests, and monotonic revision.
    - Stale observations are explicit and cannot be mistaken for fresh state.
    - Serialized model-facing state contains only allowlisted public fields and digests/references, never raw prompt, credential, private truth, or full payload values.
    - Atomic persistence uses a same-directory temporary file, flush/fsync, and replace; a failed write preserves the prior valid checkpoint.
  </behavior>
  <action>Implement dependency-light dataclasses or equivalent typed models with deterministic JSON conversion, validation, monotonic revision checks, provenance records, stale-observation markers, and checkpoint envelope metadata. Add the reusable atomic checkpoint writer here, following the repository's same-directory temporary-file, flush/fsync, and `os.replace` convention, so dependent Plan 02 never relies on an implementation introduced later. Keep field names stable within this phase, avoid SDK/framework dependencies, and cite D-01 and D-03.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_task_state.py</automated></verify>
  <done>All schema and atomic-write behaviors pass; malformed/non-monotonic checkpoints are rejected and interrupted writes preserve the prior valid checkpoint.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Trace/tool event to snapshot | Untrusted or oversized event data is reduced to allowlisted public state. |
| Snapshot/checkpoint to model/telemetry | Privacy boundary excludes credentials, prompts, private truth, and payload bodies. |
| T-01-01 | I | Snapshot serializer | mitigate | Allowlist fields and digest/reference evidence; test forbidden keys. |
| T-01-SC | Tampering | Dependencies | accept | No new package-manager dependencies in this plan. |
</threat_model>

<verification>Run focused schema tests, then full ruff and standalone pytest during phase closeout.</verification>
<success_criteria>Typed snapshots and checkpoints round-trip with all required fields, enforce revision/staleness semantics, and prove privacy exclusions.</success_criteria>
