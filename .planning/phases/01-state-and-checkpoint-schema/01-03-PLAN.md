---
phase: 01-state-and-checkpoint-schema
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - roboclaws/agents/household_live_lifecycle.py
  - tests/unit/agents/test_live_runtime_contracts.py
autonomous: true
requirements:
  - REQ-state-and-checkpoint-schema
must_haves:
  truths:
    - "A checkpoint artifact is atomically replaced at meaningful tool/action boundaries and context-budget interruption."
    - "Existing trace, report, DINO, and run_result artifacts remain readable and unchanged in shape."
  artifacts:
    - path: roboclaws/agents/household_live_lifecycle.py
      provides: "Lifecycle checkpoint calls at action/interruption boundaries"
    - path: tests/unit/agents/test_live_runtime_contracts.py
      provides: "Separate context-interruption checkpoint and completion regression proof"
  key_links:
    - from: roboclaws/agents/household_live_lifecycle.py
      to: roboclaws/agents/task_state.py
      via: "snapshot projection and checkpoint persistence"
    - from: roboclaws/agents/household_live_lifecycle.py
      to: run_dir/checkpoint.json
      via: "Plan 01 atomic writer on classified context interruption"
---

<objective>Persist the canonical snapshot as an atomic checkpoint at lifecycle boundaries while retaining existing completion semantics.</objective>
<context>@.planning/phases/01-state-and-checkpoint-schema/01-CONTEXT.md
@roboclaws/agents/household_live_lifecycle.py
@roboclaws/agents/task_state.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire atomic checkpoint persistence</name>
  <files>roboclaws/agents/household_live_lifecycle.py, tests/unit/agents/test_live_runtime_contracts.py</files>
  <behavior><br/>- Successful meaningful tool/action boundaries write the latest checkpoint atomically.<br/>- `provider_context_budget_exceeded` writes a checkpoint before terminal diagnostics.<br/>- Interrupted writes leave the prior complete checkpoint intact; no partial file is observed.<br/>- Terminal `done`/`run_result.json` behavior and existing trace/report artifacts remain unchanged.</behavior>
  <action>Call Plan 01's atomic checkpoint writer after the context-budget failure is classified, as a distinct lifecycle path independent of Plan 02's per-tool projection hook. Keep checkpoint naming/schema metadata within the established run directory without changing `trace.jsonl`, `run_result.json`, or completion ownership. Ensure write failures are surfaced, not silently replaced by baseline. Cite D-02 and D-03.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_live_runtime_contracts.py</automated></verify>
  <done>Atomicity, separate interruption coverage, privacy, and no-regression tests pass for normal and context-budget interruption paths.</done>
</task>
</tasks>
<threat_model>Run directory is a persistence boundary. T-01-03 Tampering/availability: mitigate with temp-file fsync plus replace and preservation of prior checkpoint on failure. No new dependencies.</threat_model>
<verification>Run focused lifecycle tests, then `ruff check .`, `ruff format --check .`, and standalone full pytest.</verification>
<success_criteria>Valid checkpoints are durably written at required boundaries, invalid writes fail loudly, and existing complete artifacts remain compatible.</success_criteria>
