---
phase: 01-state-and-checkpoint-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - roboclaws/agents/drivers/openai_agents_event_projection.py
  - roboclaws/agents/drivers/openai_agents_live.py
  - tests/unit/agents/test_openai_agents_event_projection.py
  - tests/unit/agents/test_openai_agents_event_log.py
autonomous: true
requirements:
  - REQ-state-and-checkpoint-schema
must_haves:
  truths:
    - "Successful MCP/tool responses update the typed snapshot using public semantic fields and evidence digests."
    - "Projection is deterministic and leaves full trace/artifact bodies in their existing storage."
  artifacts:
    - path: roboclaws/agents/drivers/openai_agents_event_projection.py
      provides: "Event-to-snapshot projection helpers"
    - path: tests/unit/agents/test_openai_agents_event_projection.py
      provides: "Projection and privacy regression tests"
  key_links:
    - from: roboclaws/agents/drivers/openai_agents_event_projection.py
      to: roboclaws/agents/task_state.py
      via: "projection returns updated TaskSnapshot"
    - from: roboclaws/agents/drivers/openai_agents_live.py
      to: roboclaws/agents/drivers/openai_agents_event_projection.py
      via: "normalized MCP/tool result callback before SDK result completion"
---

<objective>Project successful public MCP/tool events into the canonical snapshot without creating a second ledger.</objective>
<context>@.planning/phases/01-state-and-checkpoint-schema/01-CONTEXT.md
@roboclaws/agents/drivers/openai_agents_event_projection.py
@roboclaws/agents/drivers/openai_agents_event_log.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add semantic event projection</name>
  <files>roboclaws/agents/drivers/openai_agents_event_projection.py, roboclaws/agents/drivers/openai_agents_live.py, tests/unit/agents/test_openai_agents_event_projection.py, tests/unit/agents/test_openai_agents_event_log.py</files>
  <behavior><br/>- Navigation, observe, pick/place, safety, completion, and evidence-bearing responses update only corresponding allowlisted snapshot fields.<br/>- Unknown, failed, private, or oversized payload fields are ignored or represented by bounded digest/reference metadata.<br/>- Each accepted event advances revision exactly once and records provenance.<br/>- Telemetry with sentinel private scoring truth, credentials, raw prompt, and full payload emits none of those values while retaining allowed evidence digests/references.</behavior>
  <action>Add a pure projection function over existing normalized event shapes, reusing current JSON-safe/hash helpers where appropriate. In `openai_agents_live.py`, attach the projection/persist callback at the per-tool-result event-producing integration hook (the normalized MCP response/event recorder), so every successful normalized tool result updates and atomically persists a checkpoint before SDK completion. Add the sentinel telemetry assertion in `test_openai_agents_event_log.py`. Keep interruption checkpointing as a separate lifecycle path in Plan 03. Do not alter trace schema or expose evaluator-private fields; cite D-02 and D-03.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_openai_agents_event_projection.py tests/unit/agents/test_openai_agents_event_log.py</automated></verify>
  <done>Multiple successful tool results produce monotonically revised checkpoint snapshots before SDK completion; projection and telemetry tests cover representative events, stale observations, unknown events, and sentinel forbidden-data exclusion.</done>
</task>
</tasks>
<threat_model>Projection is an allowlist boundary; malformed input is rejected or ignored without copying payload bodies. T-01-02 Tampering: mitigate via deterministic normalization and privacy regression tests.</threat_model>
<verification>Focused projection tests plus full suite at phase closeout.</verification>
<success_criteria>Every supported successful event can update the snapshot deterministically while existing trace artifacts remain unchanged.</success_criteria>
