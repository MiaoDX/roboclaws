---
phase: 04-route-proof-and-rollout
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [tests, docs/status/active]
autonomous: true
requirements: [REQ-route-proof-and-rollout]
must_haves:
  truths:
    - "Camera-grounded/DINO, operator-console, and Agibot resolver metadata consume the same context contract without new launch axes."
    - "Operator-facing status identifies route, lane, baseline, privacy, and artifact evidence consistently."
  artifacts:
    - path: "tests"
      provides: "Integration contract tests for DINO, operator console, and Agibot metadata"
    - path: "docs/status/active"
      provides: "Route integration evidence"
  key_links:
    - from: "operator console and Agibot adapters"
      to: "shared context contract"
      via: "existing SDK/runtime adapter calls"
---

<objective>Prove every supported household integration uses the shared state-first context contract and preserves operator-reviewable metadata.</objective>
<execution_context>
@.planning/ROADMAP.md
@.planning/phases/04-route-proof-and-rollout/04-01-PLAN.md
@.planning/phases/04-route-proof-and-rollout/CONTEXT.md
@docs/plans/2026-09-01-state-first-context-manager.md
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Cover camera-grounded and DINO integration</name>
  <files>tests/*camera*; tests/*dino*; docs/status/active/*camera*</files>
  <action>Exercise the existing camera-grounded label lane and Grounding DINO artifact path through the SDK context assembler. Assert image/label evidence is subgoal-scoped, digests are preserved, and no additional launch axis or profile picker is introduced.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests -k 'camera or dino or grounding'</automated></verify>
  <done>Camera/DINO contract tests pass with reviewable artifact references and privacy assertions.</done>
</task>
<task type="auto">
  <name>Task 2: Cover operator-console and Agibot metadata routes</name>
  <files>tests/*operator*; tests/*agibot*; docs/status/active/*route-proof*</files>
  <action>Use existing operator-console and Agibot resolver/map-build fixtures to verify route metadata, status, and resolver provenance are projected through the same context contract. Reject retired engines or ad-hoc launch axes; preserve actionable status for missing metadata.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests -k 'operator or agibot or resolver or metadata'</automated></verify>
  <done>Integration assertions prove operator-console and Agibot paths share context semantics and retain provenance without contract expansion.</done>
</task>
<task type="auto">
  <name>Task 3: Verify the operator-console product flow</name>
  <files>none (product-run/browser verification only)</files>
  <what-built>Operator-console route metadata and shared-context integration proof.</what-built>
  <action>Launch the existing operator console and use automated browser QA to inspect the household MapBuild route without enabling physical movement. Assert the route values and generated command, readiness states, disabled movement controls, preview assets, browser console/network health, and desktop/mobile layout. This product-run acceptance gate is not permission to change console code or safety policy.</action>
  <how-to-verify>Run `just console::run`, open the printed local URL with the repo browser QA route, select the existing household MapBuild route, and assert `openai-agents-sdk`, the existing evidence lane and camera labeler axes, layered readiness, canonical launch command, disabled movement, loaded previews, no failed requests or console errors, and no responsive overflow. Capture desktop and mobile screenshots, then stop the console.</how-to-verify>
  <verify><automated>Headless browser assertions and desktop/mobile screenshots against `just console::run`</automated></verify>
  <done>Automated product QA proves route metadata, readiness, existing axes, assets, responsive rendering, and disabled movement match the shared contract.</done>
</task>
</tasks>
<threat_model>
| Boundary | Description |
|---|---|
| camera/provider -> runtime | Image labels and metadata enter the context projection |
| runtime -> operator console | Status and evidence are exposed to operators |
| TOS/artifact -> evaluator | DINO and report references are consumed |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-03 | Information disclosure | camera/operator projections | mitigate | assert redaction and bounded evidence fields in integration fixtures |
| T-04-04 | Spoofing | Agibot resolver metadata | mitigate | require provenance and digest validation before projection |
| T-04-SC | Tampering | dependency surface | mitigate | locked environment plus lint and focused tests |
</threat_model>
<verification>Run focused integration tests, then full standalone pytest and lint/format gates.</verification>
<success_criteria>All named integrations pass against the shared contract with no new launch axes and complete evidence metadata.</success_criteria>
<output>Create `.planning/phases/04-route-proof-and-rollout/04-02-SUMMARY.md` when executed.</output>
