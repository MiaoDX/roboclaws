---
phase: 04-route-proof-and-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests, scripts, docs/status/active]
autonomous: true
requirements: [REQ-route-proof-and-rollout]
must_haves:
  truths:
    - "Cleanup, MapBuild, and no-preset open-ended SDK matrices compare managed runs to an explicit unmanaged baseline."
    - "Synthetic growth proves context budget, privacy, and content-addressed artifact invariants without provider access."
  artifacts:
    - path: "tests"
      provides: "Focused route, privacy, budget, baseline, and digest assertions"
    - path: "docs/status/active"
      provides: "Reviewable proof manifest and guarded blocker records"
  key_links:
    - from: "focused test matrix"
      to: "run traces/reports/DINO artifacts"
      via: "digest and baseline manifest validation"
---

<objective>
Build the deterministic proof packet for the shared context contract across cleanup, MapBuild, and no-preset open-ended SDK routes.

Purpose: Establish reproducible evidence before any provider-backed rollout, including explicit unmanaged `baseline`, privacy boundaries, budget bounds, and content-addressed artifacts.
Output: Focused tests/scripts and a checked-in proof manifest documenting pass/fail/blocker outcomes.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-route-proof-and-rollout/CONTEXT.md
@.planning/phases/04-route-proof-and-rollout/RESEARCH.md
@docs/plans/2026-09-01-state-first-context-manager.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic cross-route matrix assertions</name>
  <files>tests/*context*; tests/*agent_sdk*; scripts/*eval* (use existing owners)</files>
  <action>Extend the existing focused harness, preserving public launch axes, to exercise cleanup, MapBuild, and no-preset open-ended `openai-agents-sdk` cases. Require an explicit unmanaged `baseline` row for every comparable case; assert managed context reconstruction stays under hard budget and never retries the identical over-limit payload. Keep camera/DINO input represented as a lane fixture without inventing a new axis.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests -k 'context or baseline or mapbuild or cleanup or open_ended'</automated></verify>
  <done>Matrix tests fail closed when baseline is missing, prove all three routes and budget behavior, and pass against existing fixtures.</done>
</task>
<task type="auto">
  <name>Task 2: Enforce privacy and content-addressed evidence checks</name>
  <files>tests/*privacy*; tests/*artifact*; docs/status/active/*route-proof*</files>
  <action>Reuse existing artifact scanners and digest helpers to assert model-input and telemetry contain no private scoring truth, credentials, raw prompts, or full tool payloads. Verify complete traces, reports, and DINO artifacts retain stable content-addressed references and remain readable. Emit a concise proof manifest with each route, baseline, digest, privacy, and budget result.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests -k 'privacy or digest or artifact or trace or report'</automated></verify>
  <done>Privacy violations and digest drift are actionable test failures; manifest records observable pass results and any guarded unavailable gate.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| runtime -> model/telemetry | Untrusted event and provider data are projected into bounded context and status |
| artifact store -> reviewer | Trace/report/DINO files are read by evaluators |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Information disclosure | context/telemetry projection | mitigate | privacy scanner rejects credentials, raw prompts, private scoring, and full payloads |
| T-04-02 | Tampering | content-addressed artifacts | mitigate | recompute digests and reject mismatched references |
| T-04-SC | Tampering | test/runtime dependencies | mitigate | use existing locked dependencies and run lint/test gates |
</threat_model>

<verification>ruff check .; ruff format --check .; focused pytest commands above; inspect proof manifest for explicit baseline and blocker fields.</verification>
<success_criteria>Deterministic evidence covers all required routes, baseline comparison, privacy boundaries, budget bounds, and content-addressed artifacts with no code outside proof/test owners.</success_criteria>

<output>Create `.planning/phases/04-route-proof-and-rollout/04-01-SUMMARY.md` when executed.</output>
