---
phase: 04-route-proof-and-rollout
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [docs/status/active, .planning/phases/04-route-proof-and-rollout/04-VALIDATION.md]
autonomous: true
requirements: [REQ-route-proof-and-rollout]
must_haves:
  truths:
    - "Focused eval recommendation/execution and conditional live product proofs produce reviewable rollout evidence."
    - "Unavailable provider, network, simulator, GPU, or credential prerequisites produce guarded blocker output rather than silent substitution."
  artifacts:
    - path: "docs/status/active"
      provides: "Rollout decision packet with live results or concrete blocker"
    - path: ".planning/phases/04-route-proof-and-rollout/04-VALIDATION.md"
      provides: "Nyquist validation ledger for deterministic, eval, product-run, and live gates"
  key_links:
    - from: "eval gates"
      to: "rollout decision packet"
      via: "baseline/managed comparison and blocker schema"
---

<objective>Run the final focused eval and conditional live rollout proof, recording either accepted evidence or guarded blockers.</objective>
<execution_context>
@.planning/ROADMAP.md
@.planning/phases/04-route-proof-and-rollout/04-02-PLAN.md
@docs/plans/2026-09-01-state-first-context-manager.md
@docs/agents/operating-runbook.md
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Execute focused eval gates and compare baseline</name>
  <files>docs/status/active/*route-proof*; docs/status/active/*decision*</files>
  <action>Run `just agent::eval recommend` and the approved focused `just agent::eval execute` for the source plan. Confirm every managed row has an explicit unmanaged `baseline`, compare quality/budget/artifact/privacy outcomes, and write the decision packet without claiming unsupported speed or quality.</action>
  <verify><automated>just agent::eval recommend plan=docs/plans/2026-09-01-state-first-context-manager.md budget=focused && just agent::eval execute plan=docs/plans/2026-09-01-state-first-context-manager.md budget=focused</automated></verify>
  <done>Eval packet contains route-level results, explicit baseline comparisons, artifact digests, privacy status, and scoped claims.</done>
</task>
<task type="auto">
  <name>Task 2: Run or guard the live product proof</name>
  <files>docs/status/active/*route-proof*; docs/status/active/*blocker*</files>
  <action>Run readiness probes in order: `scripts/dev/network_status.sh`; provider readiness through `just agent::eval recommend plan=docs/plans/2026-09-01-state-first-context-manager.md budget=focused`; and `python -m roboclaws.household.visual_grounding_sidecar.readiness --pipeline grounding-dino`. When all pass, execute the cheapest existing camera-grounded product proof: `just run::surface surface=household-world agent_engine=direct-runner preset=map-build evidence_lane=camera-grounded-labels camera_labeler=grounding-dino`. Validate completion, real detector provenance, composite observation, public handles, and content-addressed run/DINO/report artifacts. If a probe fails, write the command, exit code, sanitized stdout/stderr, timestamp, and `RESULT_STATUS: BLOCKED_NEEDS_LOCAL_VALIDATION` plus `BLOCKER_KIND: network|provider|grounding-dino|runtime` and `BLOCKED_COMMAND: <exact command>` to the blocker receipt; stop without substituting simulator labels, another provider, baseline, or launch axis.</action>
  <verify><automated>scripts/dev/network_status.sh && python -m roboclaws.household.visual_grounding_sidecar.readiness --pipeline grounding-dino && just run::surface surface=household-world agent_engine=direct-runner preset=map-build evidence_lane=camera-grounded-labels camera_labeler=grounding-dino</automated></verify>
  <done>Live evidence is captured when prerequisites pass; otherwise a concrete guarded blocker is recorded and rollout is explicitly incomplete.</done>
</task>
<task type="auto">
  <name>Task 3: Complete the Phase 4 validation ledger</name>
  <files>.planning/phases/04-route-proof-and-rollout/04-VALIDATION.md</files>
  <action>Create the Nyquist validation artifact mapping `REQ-route-proof-and-rollout` and every roadmap success criterion to deterministic, focused eval, operator-console product-run/manual, and conditional live evidence. Record exact commands, exit status, artifact paths/digests, baseline identities, privacy results, and accepted claim scope. A required gate is PASS only with observable evidence; use BLOCKED_NEEDS_LOCAL_VALIDATION with the exact blocker receipt for unavailable live prerequisites, and never mark the phase complete from deterministic proxies alone.</action>
  <verify><automated>test -s .planning/phases/04-route-proof-and-rollout/04-VALIDATION.md && rg -q 'REQ-route-proof-and-rollout' .planning/phases/04-route-proof-and-rollout/04-VALIDATION.md && rg -q 'deterministic|focused eval|operator-console|camera-grounded|BLOCKED_NEEDS_LOCAL_VALIDATION' .planning/phases/04-route-proof-and-rollout/04-VALIDATION.md</automated></verify>
  <done>The validation ledger explicitly accounts for every required gate and supports either a fully evidenced PASS or an exact guarded live blocker, with no unproven completion claim.</done>
</task>
</tasks>
<threat_model>
| Boundary | Description |
|---|---|
| environment/provider -> live eval | External credentials, network, runtime, and simulator state affect proof |
| eval artifacts -> rollout decision | Results could be overclaimed or misattributed |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-05 | Repudiation | eval decision packet | mitigate | include command, timestamp, baseline identity, digest, and scoped claim |
| T-04-06 | Denial of service | live provider/runtime | accept | guarded preflight records unavailable resources without retries or scope expansion |
| T-04-SC | Tampering | execution dependencies | mitigate | locked environment, lint, and standalone tests before live gate |
</threat_model>
<verification>Run lint, format, standalone pytest, focused eval commands, operator-console product flow, network/provider/DINO readiness probes, and conditional camera-grounded MapBuild; reconcile every result in `04-VALIDATION.md`.</verification>
<success_criteria>Phase is rollout-ready only with deterministic plus eval plus available live proof; unavailable live prerequisites are explicitly BLOCKED_NEEDS_LOCAL_VALIDATION.</success_criteria>
<output>Create `.planning/phases/04-route-proof-and-rollout/04-03-SUMMARY.md` when executed.</output>
