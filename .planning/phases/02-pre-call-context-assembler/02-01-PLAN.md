---
phase: 02-pre-call-context-assembler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roboclaws/agents/drivers/openai_agents_context_assembler.py
  - roboclaws/agents/drivers/openai_agents_budget.py
  - tests/unit/agents/test_openai_agents_context_assembler.py
  - tests/unit/agents/test_openai_agents_budget_sources.py
autonomous: true
requirements:
  - REQ-pre-call-context-assembler
must_haves:
  truths:
    - "A managed input can be rebuilt from the latest Phase 1 checkpoint, fixed instructions, subgoal evidence, and bounded recent raw overlap."
    - "Admission always accounts for estimated input, provider/lane expected output, and safety reserve against the hard limit."
    - "Eviction is deterministic and removes optional retrieval before oldest raw overlap while retaining all action-critical snapshot fields."
  artifacts:
    - path: roboclaws/agents/drivers/openai_agents_context_assembler.py
      provides: "Pure snapshot-backed context assembly, estimation, reserve accounting, and ordered eviction"
    - path: tests/unit/agents/test_openai_agents_context_assembler.py
      provides: "Focused reconstruction and eviction behavior tests"
    - path: tests/unit/agents/test_openai_agents_budget_sources.py
      provides: "Reserve arithmetic and conservative estimation tests"
  key_links:
    - from: roboclaws/agents/drivers/openai_agents_context_assembler.py
      to: roboclaws/agents/task_state.py
      via: "Checkpoint/TaskSnapshot read-only projection"
    - from: roboclaws/agents/drivers/openai_agents_context_assembler.py
      to: roboclaws/agents/drivers/openai_agents_budget.py
      via: "shared budget limits and failure metadata"
---

<objective>
Define the dependency-light pre-call assembler and budget policy that turns Phase 1 checkpoint state into a bounded model input.

Purpose: Establish one testable source of truth for reconstruction, reserves, and eviction before wiring it into the SDK callback.
Output: Assembler module plus focused unit coverage for reconstruction, reserve equation, watermark decisions, ordered eviction, and privacy-preserving snapshot retention.
</objective>

<execution_context>
@/home/mi/.codex/get-shit-done/workflows/execute-plan.md
@roboclaws/agents/task_state.py
@roboclaws/agents/drivers/openai_agents_budget.py
@roboclaws/agents/drivers/openai_agents_compaction.py
@.planning/phases/01-state-and-checkpoint-schema/01-CONTEXT.md
</execution_context>

<context>
Implement D-01/D-02/D-03 from the Phase 1 research constraints: consume existing checkpoint/artifact state, never add a ledger, and keep privacy filtering and provenance intact. Honor Phase 2 locked decisions: reconstruction precedes the hard guard; use conservative injectable estimation; soft watermark is proactive; fixed instructions and action-critical snapshot fields are non-evictable; no provider-native compaction or public contract changes.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build snapshot-backed assembler and budget value types</name>
  <files>roboclaws/agents/drivers/openai_agents_context_assembler.py, roboclaws/agents/drivers/openai_agents_budget.py, tests/unit/agents/test_openai_agents_context_assembler.py</files>
  <behavior>
    - A checkpoint plus fixed instructions, current subgoal evidence, optional retrieval, and recent raw items produces deterministic ordered input.
    - Soft-watermark crossing requests reconstruction before hard-limit admission, even when prior observed input is below hard limit.
    - The admission result exposes estimated input, expected output, safety reserve, hard limit, and whether reconstruction/eviction occurred.
  </behavior>
  <action>Create a pure assembler API with explicit policy/result dataclasses. Read `Checkpoint`/`TaskSnapshot` through existing serialization APIs and include only public snapshot fields, evidence references/digests, bounded raw overlap, and fixed system contract. Add conservative token estimation and provider/lane reserve selection as injectable policy values; validate numeric limits and reject invalid soft&gt;hard configurations. Keep legacy budget failure helpers compatible and do not add launch axes or schema fields.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_openai_agents_context_assembler.py</automated></verify>
  <done>Assembler returns deterministic reconstructed input and observable budget components; privacy-sensitive/raw payload fields are excluded; watermark and hard-limit decisions are test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove reserve arithmetic and ordered eviction</name>
  <files>tests/unit/agents/test_openai_agents_budget_sources.py, tests/unit/agents/test_openai_agents_context_assembler.py</files>
  <behavior>
    - Expected output plus safety reserve causes rejection when their sum would exceed hard limit.
    - Optional targeted retrieval is evicted before oldest raw overlap, with stable ordering.
    - Pose, waypoint, object freshness/provenance, action outcomes, safety, completion, and evidence digests survive all eviction.
    - Synthetic growth remains under hard limit after allowed eviction.
  </behavior>
  <action>Add focused fixtures and assertions for provider/lane reserve defaults, conservative estimator behavior, ordered eviction, action-critical retention, and synthetic growth. Assert emitted result metadata is aggregate-only and does not contain prompts, credentials, private scoring truth, or full tool payloads.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_openai_agents_budget_sources.py tests/unit/agents/test_openai_agents_context_assembler.py</automated></verify>
  <done>All reserve, eviction, retention, privacy, and bounded-growth behaviors are deterministic and regression-tested.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| checkpoint/artifacts -> model input | Durable run data crosses into provider-facing context |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Information Disclosure | assembler projection | mitigate | Project through TaskSnapshot public serialization and evidence digests only; test forbidden fields |
| T-02-02 | Denial of Service | budget admission | mitigate | Validate limits, reserve output/safety, ordered eviction, and fail closed on residual overflow |
| T-02-SC | Tampering | dependency installs | accept | No new packages or installs in this plan |
</threat_model>

<verification>Run the two focused pytest commands above plus `ruff check` on changed Python files.</verification>

<success_criteria>Assembler policy and tests prove pre-call reconstruction inputs, reserve equation, watermark behavior, ordered eviction, critical-state retention, and privacy bounds without changing public contracts.</success_criteria>

<output>Create `.planning/phases/02-pre-call-context-assembler/02-01-SUMMARY.md` when executed.</output>
