---
phase: 02-pre-call-context-assembler
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - roboclaws/agents/drivers/openai_agents_compaction.py
  - roboclaws/agents/drivers/openai_agents_run_config.py
  - tests/unit/agents/test_openai_agents_context_assembler.py
  - tests/unit/agents/test_live_runtime_budget.py
autonomous: true
requirements:
  - REQ-pre-call-context-assembler
must_haves:
  truths:
    - "Every managed SDK model call reconstructs and admits context before the legacy hard guard and provider dispatch."
    - "Residual required-content overflow writes one Phase 1 checkpoint, emits actionable budget evidence, and does not retry the identical payload."
    - "Existing item-level compaction remains subordinate and existing traces/reports/artifacts and public MCP/launch contracts remain unchanged."
  artifacts:
    - path: roboclaws/agents/drivers/openai_agents_compaction.py
      provides: "Pre-call assembler invocation and residual fail-closed handling"
    - path: tests/unit/agents/test_live_runtime_budget.py
      provides: "Integration-level budget event/checkpoint assertions"
  key_links:
    - from: roboclaws/agents/drivers/openai_agents_compaction.py
      to: roboclaws/agents/drivers/openai_agents_context_assembler.py
      via: "model-input filter invokes reconstruction before guard"
    - from: roboclaws/agents/drivers/openai_agents_compaction.py
      to: roboclaws/agents/drivers/openai_agents_budget.py
      via: "residual budget failure/checkpoint path"
---

<objective>
Wire the Phase 2 assembler into the OpenAI Agents SDK model-input filter and prove residual overflow fails closed exactly once.

Purpose: Make the pre-call contract effective on every managed invocation while preserving legacy compaction as a subordinate detail and keeping Phase 3 continuation out of scope.
Output: Hook integration, checkpoint-once residual path, and focused integration tests.
</objective>

<execution_context>
@/home/mi/.codex/get-shit-done/workflows/execute-plan.md
@roboclaws/agents/drivers/openai_agents_compaction.py
@roboclaws/agents/drivers/openai_agents_run_config.py
@roboclaws/agents/drivers/openai_agents_budget.py
@roboclaws/agents/task_state.py
</execution_context>

<context>
Implement locked decisions D-01/D-02/D-03 and Phase 2 decisions exactly: invoke reconstruction before the existing hard-limit guard/provider call; preserve current MCP/runtime ownership, traces, reports, DINO artifacts, and launch axes; residual overflow checkpoints once and raises actionable `provider_context_budget_exceeded` without replay. Do not implement continuation/resume classification (Phase 3), route rollout (Phase 4), provider-native compaction, or public/durable schema changes.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Integrate reconstruction into the model-input filter</name>
  <files>roboclaws/agents/drivers/openai_agents_compaction.py, roboclaws/agents/drivers/openai_agents_run_config.py, tests/unit/agents/test_openai_agents_context_assembler.py</files>
  <behavior>
    - Filter calls assembler before `_raise_budget_failure_before_model_call` and before provider dispatch.
    - Reconstructed items, not the prior full transcript, are passed to subordinate `_compact_model_input_items`.
    - Existing unmanaged baseline and public run configuration behavior remain unchanged.
  </behavior>
  <action>Update the filter wiring to load the latest checkpoint and invoke the Plan 01 assembler, then apply existing item compaction only to the assembled payload. Preserve model-data/instruction adapters, existing metrics, and profile keys; add no launch axis. Ensure missing required checkpoint state produces the existing actionable fail-closed error rather than silently falling back to baseline.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_openai_agents_context_assembler.py tests/unit/agents/test_openai_agents_input_window.py</automated></verify>
  <done>Call ordering is regression-tested and every managed filter invocation uses reconstructed bounded context before hard guard/provider dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and test residual overflow checkpoint-once behavior</name>
  <files>roboclaws/agents/drivers/openai_agents_compaction.py, tests/unit/agents/test_live_runtime_budget.py</files>
  <behavior>
    - Required content plus reserves over hard limit writes the Phase 1 checkpoint exactly once and emits actionable aggregate budget evidence.
    - The same oversized payload is never retried or dispatched to the provider.
    - Successful bounded growth preserves existing artifact/trace behavior.
  </behavior>
  <action>Connect assembler residual-overflow results to the existing atomic checkpoint writer and `provider_context_budget_exceeded` failure/event projection. Guard against duplicate writes/replay within one filter invocation, preserve terminal artifact semantics, and keep all evidence privacy-bounded. Add synthetic-growth tests that assert one checkpoint write, one failure event, zero provider calls, and no identical-payload retry.</action>
  <verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_live_runtime_budget.py tests/unit/agents/test_openai_agents_context_assembler.py</automated></verify>
  <done>Residual overflow fails closed once with checkpoint and evidence; bounded inputs proceed; no continuation policy or public contract changes are introduced.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| model-input filter -> provider | Assembled context is dispatched to an external model provider |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03 | Denial of Service | filter/provider dispatch | mitigate | Admit only after reserve equation and ordered eviction; residual overflow raises before dispatch |
| T-02-04 | Information Disclosure | filter telemetry | mitigate | Reuse aggregate budget event projection; never log raw prompt/tool payloads |
| T-02-SC | Tampering | dependency installs | accept | No new packages or installs in this plan |
</threat_model>

<verification>Run focused pytest commands above, `ruff check` and `ruff format --check` for changed files, then the documented full standalone suite if time permits.</verification>

<success_criteria>Managed calls reconstruct before hard admission, remain bounded with reserves and ordered eviction, and residual overflow checkpoints/fails closed once without replay while existing artifacts and contracts remain intact.</success_criteria>

<output>Create `.planning/phases/02-pre-call-context-assembler/02-02-SUMMARY.md` when executed.</output>
