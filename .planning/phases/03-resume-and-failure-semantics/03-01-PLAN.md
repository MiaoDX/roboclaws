---
phase: 03-resume-and-failure-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roboclaws/agents/household_live_continuation.py
  - roboclaws/agents/household_live_lifecycle.py
  - tests/unit/agents/test_household_live_continuation.py
  - tests/unit/agents/test_household_live_lifecycle.py
autonomous: true
requirements:
  - REQ-resume-and-failure-semantics
must_haves:
  truths:
    - "Valid checkpointed context overflow resumes from reconstructed state without replaying the oversized payload."
    - "Missing, corrupt, terminal, non-context, and exhausted states fail closed with actionable reason codes."
  artifacts:
    - path: roboclaws/agents/household_live_continuation.py
      provides: "Bounded checkpoint resumability classification and continuation policy"
    - path: tests/unit/agents/test_household_live_continuation.py
      provides: "Focused valid and invalid continuation semantics coverage"
  key_links:
    - from: roboclaws/agents/household_live_continuation.py
      to: roboclaws/agents/task_state.py
      via: "Validated checkpoint load and snapshot projection"
---

<objective>Classify checkpointed context overflow and resume valid non-terminal runs from reconstructed state with bounded continuations.</objective>
<context>@.planning/phases/03-resume-and-failure-semantics/03-CONTEXT.md
@roboclaws/agents/household_live_lifecycle.py
@roboclaws/agents/household_live_continuation.py
@roboclaws/agents/task_state.py</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add resumability classification and bounded continuation policy</name>
<files>roboclaws/agents/household_live_continuation.py, roboclaws/agents/household_live_lifecycle.py, tests/unit/agents/test_household_live_continuation.py</files>
<behavior>
- Valid checkpoint plus `provider_context_budget_exceeded` and no terminal completion is resumable.
- Missing/corrupt, terminal, non-context, and exhausted cases fail closed with reason codes.
- Continuation uses checkpoint-backed reconstructed input and never the oversized payload.
</behavior>
<action>Reuse Phase 1 readers and Phase 2 assembler. Add a small internal classification seam at the existing continuation owner, persist aggregate status and checkpoint digest, reject terminal snapshots, and enforce one bounded continuation without baseline fallback.</action>
<verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_household_live_continuation.py</automated></verify>
<done>Valid, invalid, terminal, and exhausted cases are deterministic, bounded, and privacy-tested.</done>
</task>
</tasks>
<verification>Run focused continuation tests and `ruff check` on changed modules.</verification>
<success_criteria>Valid overflow resumes exactly once within the bound; invalid classes fail closed with reason codes.</success_criteria>
<output>Create `.planning/phases/03-resume-and-failure-semantics/03-01-SUMMARY.md` when executed.</output>
