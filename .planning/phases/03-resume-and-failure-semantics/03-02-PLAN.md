---
phase: 03-resume-and-failure-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - roboclaws/agents/household_live_lifecycle.py
  - roboclaws/agents/household_live_continuation.py
  - tests/unit/agents/test_household_live_lifecycle.py
  - tests/unit/agents/test_live_runtime_budget.py
autonomous: true
requirements:
  - REQ-resume-and-failure-semantics
must_haves:
  truths:
    - "Terminal completion is never resumed and success still requires MCP done plus run_result.json."
    - "Recoverable and unrecoverable outcomes are distinct in aggregate status and artifacts."
  artifacts:
    - path: roboclaws/agents/household_live_lifecycle.py
      provides: "Lifecycle completion guards and status/artifact projection"
    - path: tests/unit/agents/test_household_live_lifecycle.py
      provides: "Terminality, one-shot projection, and privacy regression coverage"
  key_links:
    - from: roboclaws/agents/household_live_lifecycle.py
      to: roboclaws/agents/household_live_continuation.py
      via: "Continuation classification gates lifecycle resume"
---

<objective>Prove terminal success and artifact semantics remain fail-closed while resumable and unrecoverable outcomes are distinguishable.</objective>
<context>@.planning/phases/03-resume-and-failure-semantics/03-CONTEXT.md
@.planning/phases/03-resume-and-failure-semantics/03-01-PLAN.md
@roboclaws/agents/household_live_lifecycle.py</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Integrate status/artifact projection and lifecycle guards</name>
<files>roboclaws/agents/household_live_lifecycle.py, roboclaws/agents/household_live_continuation.py, tests/unit/agents/test_household_live_lifecycle.py, tests/unit/agents/test_live_runtime_budget.py</files>
<behavior>
- Terminal completion is never resumed; success still requires MCP `done` plus `run_result.json`.
- One checkpoint digest and one failure/continuation projection are emitted; no duplicate dispatch occurs.
- Recoverable, corrupt/missing, exhausted, terminal, and unrecoverable statuses are distinct and privacy-bounded.
</behavior>
<action>Wire classification into existing lifecycle completion and run-result projection. Preserve `done` terminality and artifact readers; assert write/event counts, zero duplicate dispatch, and actionable malformed-checkpoint repair guidance.</action>
<verify><automated>./scripts/dev/run_pytest_standalone.sh -q tests/unit/agents/test_household_live_lifecycle.py tests/unit/agents/test_live_runtime_budget.py</automated></verify>
<done>Lifecycle tests prove terminal invariants, one-shot projection, actionable invalid evidence, and privacy boundaries.</done>
</task>
</tasks>
<verification>Run focused tests, `ruff check .`, `ruff format --check .`, and standalone pytest at closeout.</verification>
<success_criteria>All four Phase 3 roadmap criteria are observable in deterministic lifecycle tests with existing artifacts preserved.</success_criteria>
<output>Create `.planning/phases/03-resume-and-failure-semantics/03-02-SUMMARY.md` when executed.</output>
